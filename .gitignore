__pycache__
env
cache*
output*
# The bare output* pattern above (meant for the output/ data directory) also
# matched dhaka/output.py - a pipeline SOURCE file, the Dhaka synthesis output
# stage - which was therefore never under version control. Keep it tracked.
!dhaka/output.py
raw_data*

.vscode
.idea

config_local_*.yml
euler_*.sh
slurm-*.out
*.xlsx

# Maven build output for matsim_run/ (includes an ~85MB shaded jar - rebuild
# via `mvn clean package` in matsim_run/, don't commit it)
target/

# DTCA sheet cache + estimation dataset (regenerable from raw_data via
# dhaka/mode_choice/build_estimation_dataset.py; ~6 min Excel parse)
dhaka/mode_choice/cache_dtca_sheets/
dhaka/mode_choice/estimation_dataset.parquet
